#include "circle_queue.h"
// #include "lib-sub-source/include/lib_sub_source.h"


int mian(int argc, char *[])
{
    circle_queue queue;
    uint32_t data;
    queue.lib_queue.get_data(&data);
    return 0;
}